apiVersion: v1
kind: Service
metadata:
  name: proteus
  namespace: proteus-system
spec:
  selector:
    app.kubernetes.io/name: proteus
    app.kubernetes.io/component: controller
  ports:
    - name: http
      port: 80
      targetPort: http
